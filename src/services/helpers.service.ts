import { getFixedUsername, getRoot, getUTCNow, resultFromCb } from 'helpers';

export class HelpersService {
  public getFixedUsername = getFixedUsername;
  public getRoot = getRoot;
  public getUTCNow = getUTCNow;
  public resultFromCb = resultFromCb;
}
